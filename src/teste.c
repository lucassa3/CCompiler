{
    x = scanf();
    while(x > 1 && x > 2) {
        printf(x);
        x = x - 1;
    };
    if(x >= 3) {
        printf(x);
    };
    /* if(x <= 8) {
    //     printf(x);
    // };*/
}